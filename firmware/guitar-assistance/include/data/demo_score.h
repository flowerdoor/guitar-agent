#pragma once

#include <stddef.h>
#include "models/tab_step.h"

namespace DemoScore {
    extern const ScoreModel::TabStep STEPS[];
    extern const size_t STEP_COUNT;
    extern const uint32_t END_TICK;
}
